language: python
python:
  - "3.6"
services:
  - postgresql
install:
  - pip install -r requirements.txt
  - pip install coveralls
script:
  - pytest --cov=app --cov-report=term-missing
  
after_success:
  - coveralls